
import { coffeeOrder } from 'coffeerun-ember/helpers/coffee-order';
import { module, test } from 'qunit';

module('Unit | Helper | coffee order');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = coffeeOrder([42]);
  assert.ok(result);
});

